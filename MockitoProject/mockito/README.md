```
mockito를 사용하여 junit 테스트 하기

mockito : 목 객체를 쉽게 만들 수 있는 framework이다.
목 객체 : 테스트를 진행할 때 존재하는 객체를 만들 수 없을 때 만드는 가짜 객체 

테스트하고 싶은 코드에 대해서 정확하게 테스트하기 위해서 사용된다.
1. 해당 코드가 아닌 의존관계에 있는 다른 코드에서 오류가 날 경우
2. 네트워크나 DB 연결 등 외부의 요인에 영향을 받는 경우

verify : 목 객체가 원하는 데로 생성이 됐는지 확인
```